Options -Indexes
DirectoryIndex index.html index.php

RewriteEngine On

# ── 1. Fix cPanel : supprime /index.php fantôme ──────────────────────────────
# (seulement si index.php est dans la requête visible, pas pour le vrai WP)
RewriteCond %{THE_REQUEST} \s/((?!blog/).*?)index\.php[\s?]
RewriteRule ^ /%1 [R=301,L]

# ── 2. WordPress : TOUT /blog → Apache/WP directement (son propre .htaccess) ─
RewriteRule ^blog(/.*)?$ - [L]

# ── 3. Fichiers statiques existants → servis directement ─────────────────────
# (_astro/, logo.svg, favicon.ico, robots.txt, images, etc.)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# ── 4. Dossiers avec index.html → servi directement (pages Astro statiques) ──
# /             → public_html/index.html
# /notre-agence/ → public_html/notre-agence/index.html
RewriteCond %{REQUEST_FILENAME} -d
RewriteCond %{REQUEST_FILENAME}index.html -f
RewriteRule ^(.*)$ /$1index.html [L]

# ── 5. Reste → Node.js Astro SSR (routes dynamiques du blog /blog/*) ──────────
RewriteRule ^(.*)$ http://127.0.0.1:3862/$1 [P,L]
